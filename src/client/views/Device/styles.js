import {
  StyleSheet
} from 'react-native';

export const index = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'stretch'
  },
  webView: {
    flex: 1,
    backgroundColor: 'transparent'
  }
});
